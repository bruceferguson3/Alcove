import React from "react";

const ListingProductDescription = ({ description }) => {
  return (
    <div>
      <h4>Product Description</h4>
      <p>{description}</p>
    </div>
  );
};

export default ListingProductDescription;
